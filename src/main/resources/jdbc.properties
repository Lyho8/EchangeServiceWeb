	# paramètres pour une connection mySql "classique"
	jdbc.username=root
	jdbc.password=
	jdbc.driverClassName=com.mysql.jdbc.Driver
	jdbc.url=jdbc:mysql:///dta

# paramètres pour une connection mySql par datasource
#datasource=mySqlBanqueDS
#datasource=java:comp/env/jdbc/BanqueDS

# classique <=> les donnees sont recuperees dans la base de donnees
# bouchon <=> pour le developpement uniquement.
# les donnees sont 'bouchonnees' (renvoyees statiquement sans acces en base de donnees).
acces_donnees=classique